/** Operational Ledger design reminder: the entry screen is calm and professional, setting expectations for a controlled shared workspace. */
import { ArrowRight, LockKeyhole } from "lucide-react";
import { useWorkspace } from "@/contexts/WorkspaceContext";
export default function LoginPage() { const { signIn, firebaseError } = useWorkspace(); return <main className="login-page"><section className="login-panel"><div className="login-brand"><img src="/manus-storage/action-plan-ledger-mark_105207f3.png" alt="Action Plan Planner mark" /><span>Action Plan</span></div><p className="eyebrow">Shared planning workspace</p><h1>See every allocation before it becomes a surprise.</h1><p>Use your approved Google account to enter the shared action-plan ledger. The workspace owner controls roles and planning data.</p><button className="primary-button login-button" onClick={() => void signIn()}>Continue with Google <ArrowRight size={18} /></button>{firebaseError && <p className="form-error">{firebaseError}</p>}<small><LockKeyhole size={14} />Secure access is provided by Firebase Authentication.</small></section><div className="login-art"><img src="/manus-storage/action-plan-overview-art_8c944bf7.png" alt="Abstract ledger composition" /></div></main>; }
